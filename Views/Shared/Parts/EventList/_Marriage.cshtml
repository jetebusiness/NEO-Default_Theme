@model DomainCustomer.Entities.EventList
<div class="row">
    <div class="sixteen wide computer sixteen wide tablet sixteen wide mobile column">
        @using (Html.BeginForm(Model != null && Model.IdEventList != null && Model.IdEventList > 0 ? "ManagerList" : "CreateList", "EventList", FormMethod.Post, new { name = "formEventList", id = "formEventList", enctype = "multipart/form-data" }))
        {

            <input type="hidden" name="IdClient" value="@(Session["UserAuthenticated"] != null ? ((DomainCustomer.Entities.Customer)Session["UserAuthenticated"]).IdCustomer.ToString() : "")" />
            <input type="hidden" name="Type" value="1" />

            if (Model != null && Model.IdEventList > 0)
            {
                <input type="hidden" name="IdEventList" value="@Model.IdEventList" />
            }

            <div class="ui segment">
                <h2>Configurar Dados</h2>
                <div class="ui grid stackable top aligned left grid">
                    <div class="row two column divided horizontal">
                        <div class="column">
                            <div class="ui form">
                                <h4 class="ui dividing header">Dados do Evento</h4>
                                @if(Model.IdEventList > 0)
                                {
                                    <div class="field ui toggle checkbox">
                                        <input id="chkStatus" type="checkbox" name="Status" @(Model.Status ? "checked" : "") value="@Model.Status">
                                        <label id="lblStatus" class="text bold">@(Model.Status ? "Ativo" : "Inativo")</label>
                                    </div>
                                }
                                else
                                {
                                    <div class="field ui toggle checkbox disabled">
                                        <input id="chkStatus" type="checkbox" name="Status" checked value="true">
                                        <label id="lblStatus" class="text bold">Ativo</label>
                                    </div>
                                }
                                <div class="field calendar required">
                                    <label>Data do Evento</label>
                                    <div class="ui input left icon">
                                        <i class="calendar icon"></i>
                                        <input type="text" class="masked mask_date" name="EventDate" placeholder="DD/MM/YYYY" value="@(Model != null && Model.IdEventList > 0 ? Model.EventDate.ToString() : "")" data-validate="eventdate">
                                    </div>
                                </div>
                                <div class="field required">
                                    <label>Criador da Lista</label>
                                    <div class="ui left icon input">
                                        <i class="user icon"></i>       
                                        <input maxlength="200" type="text" name="CreatorName" value="@(Model != null && Model.CreatorName != null ? Model.CreatorName.ToString() : Session["Name"])" placeholder="João das Flores" data-validate="name">
                                    </div>
                                </div>
                                <h4 class="ui dividing header">Cônjuge 1</h4>
                                <div class="field required">
                                    <label>Nome</label>
                                    <div class="ui left icon input">
                                        <i class="user icon"></i>
                                        <input maxlength="200" type="text" name="Name1" value="@(Model != null && Model.Name1 != null ? Model.Name1.ToString() : "")" placeholder="João das Flores" data-validate="groomname">
                                    </div>
                                </div>
                                <div class="field required">
                                    <label>E-mail</label>
                                    <div class="ui left icon input">
                                        <i class="mail icon"></i>
                                        <input maxlength="200" type="text" name="Email1" value="@(Model != null && Model.Email1 != null ? Model.Email1.ToString() : "")" placeholder="email@email.com.br" data-validate="groomemail">
                                    </div>
                                </div>
                                <div class="field">
                                    <label>Nome do Pai</label>
                                    <div class="ui left icon input">
                                        <i class="user icon"></i>
                                        <input maxlength="200" type="text" name="Name2" value="@(Model != null && Model.Name2 != null ? Model.Name2.ToString() : "")" placeholder="João das Flores">
                                    </div>
                                </div>
                                <div class="field">
                                    <label>Nome da Mãe</label>
                                    <div class="ui left icon input">
                                        <i class="user icon"></i>
                                        <input maxlength="200" type="text" name="Name3" value="@(Model != null && Model.Name3 != null ? Model.Name3.ToString() : "")" placeholder="Maria das Flores">
                                    </div>
                                </div>
                                <h4 class="ui dividing header">Cônjuge 2</h4>
                                <div class="field required">
                                    <label>Nome</label>
                                    <div class="ui left icon input">
                                        <i class="user icon"></i>
                                        <input maxlength="200" type="text" name="Name4" value="@(Model != null && Model.Name4 != null ? Model.Name4.ToString() : "")" placeholder="João das Flores" data-validate="bridename">
                                    </div>
                                </div>
                                <div class="field required">
                                    <label>E-mail</label>
                                    <div class="ui left icon input">
                                        <i class="mail icon"></i>
                                        <input maxlength="200" type="text" name="Email2" value="@(Model != null && Model.Email2 != null ? Model.Email2.ToString() : "")" placeholder="email@email.com.br" data-validate="brideemail">
                                    </div>
                                </div>
                                <div class="field">
                                    <label>Nome do Pai</label>
                                    <div class="ui left icon input">
                                        <i class="user icon"></i>
                                        <input maxlength="200" type="text" name="Name5" value="@(Model != null && Model.Name5 != null ? Model.Name5.ToString() : "")" placeholder="João das Flores">
                                    </div>
                                </div>
                                <div class="field">
                                    <label>Nome da Mãe</label>
                                    <div class="ui left icon input">
                                        <i class="user icon"></i>
                                        <input maxlength="200" type="text" name="Name6" value="@(Model != null && Model.Name6 != null ? Model.Name6.ToString() : "")" placeholder="Maria das Flores">
                                    </div>
                                </div>
                                @if (Model.IdEventList == 0 && !string.IsNullOrEmpty(Model.Acceptance))
                                {
                                    <div class="field required">
                                        <label>Termos e Condições</label>
                                    </div>
                                    <div class="field">
                                        <div class="ui checkbox termo aceite">
                                            <input type="checkbox" data-validate="termo">
                                        </div>
                                        <label class="inline-block" id="termoAceiteEventList">Li e estou de acordo com o regulamento. <a style="cursor: pointer;">Ler o Regulamento</a></label>
                                    </div>
                                }
                            </div>
                        </div>
                        <div class="column">
                            <div class="ui form">
                                <div class="field">
                                    <label>Mensagem aos convidados</label>
                                    <div class="ui input">
                                        <textarea maxlength="8000" name="Message">@(Model != null ? Model.Message : "")</textarea>
                                    </div>
                                </div>
                                <div class="field">
                                    <label>Imagem do Evento</label>
                                    <div class="ui image fluid">
                                        <img class="ui image fluid" id="imgPicture" src="@(Model != null ? Model.UrlPicture : "")" />
                                        <div id="uploadArea">
                                            <span>
                                                <i class="huge cloud upload icon"></i>
                                                Enviar Arquivo
                                            </span>
                                        </div>
                                    </div>
                                    <input type="file" id="filePicture" name="Picture" />
                                    <a class="ui button basic floated right" id="changeEventPicture">Alterar</a>
                                    <div class="text small">Extensões permitidas: .jpg .png ou .gif</div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <div class="row">
                        <div class="column btnEventList">
                            <a href="javascript:history.go(-1)" class="ui button large primary basic left floated text uppercase">
                                <i class="remove icon"></i> Cancelar
                            </a>
                            <button type="submit" class="ui button large submit primary right floated text uppercase">
                                <i class="checkmark icon"></i> @(Model.IdEventList > 0 ? "Salvar" : "Próximo")
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
