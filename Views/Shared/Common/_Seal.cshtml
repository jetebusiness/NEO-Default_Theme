@model List<DomainCompany.Entities.Seal>

@if (Model != null)
{
    foreach (var item in Model.FindAll(s => s.Code == null))
    {
        <a href='@item.link' target="_blank"><img class='ui image' src='@item.Image' onerror="imgError(this)"></a>
    }

    foreach (var item in Model.FindAll(s => s.Code != null))
    {
      @Html.Raw(item.Code);
    }
}